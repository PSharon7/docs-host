using System;
using System.Threading.Tasks;

namespace docs.host
{
    public static class Publish
    {
        public static Task Migrate(string basePath, string branch, string locale)
        {
            throw new NotImplementedException();
        }
    }
}
