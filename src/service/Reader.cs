using System;
using System.Threading.Tasks;

namespace docs.host
{
    public static class Reader
    {
        public static Task<Document> QueryDocument(string url, string branch, string locale, string version)
        {
            throw new NotImplementedException();
        }
    }
}
